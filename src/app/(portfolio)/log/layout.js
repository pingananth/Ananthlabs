export const metadata = {
  title: "Changelog | Ananth Labs",
  description: "A chronological log of shipped products, updates, and milestones.",
};

export default function LogLayout({ children }) {
  return <>{children}</>;
}
